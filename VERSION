1.3

# Version History

## v1.3 (2026-03-03)
- **CRITICAL FIX**: Cockpit tab looks at ROW 10 for most recent date (was checking wrong rows)
- Finds most recent date in Cockpit row 10, then copies that column to next column
- Example: BJ (row 10 has most recent date) → BK (new column)
- BTH file uses most recent date from row 30
- Added verification step: confirms value is correctly pasted to Cockpit row 20
- Expected result: 564,519,892 in Cockpit BK20

## v1.2 (2026-03-03)
- **BUG FIX**: Cockpit tab now uses same column as Beginning Balances (stays in sync)
- Fixed issue where Cockpit wasn't copying formulas correctly
- Cockpit now copies from previous column to new column using same column number as Beginning Balances
- Ensures Beginning Balances and Cockpit tabs use matching columns

## v1.1 (2026-03-03)
- Added Cockpit tab processing (copies formulas from last column to next column)
- Added BTH Investments Financing Schedule file upload (optional third file)
- BTH file integration: matches date and pastes Total BTH Financing to Cockpit row 20
- Updated UI to support 3-column file upload grid
- Enhanced processing logic with Steps 8-10

## v1.0 (2026-03-03)
- Initial versioned release
- Python Flask web application for Excel balance processing
- Formula adjustment with absolute/relative reference handling
- Special handling for references 11, 60, 76, 91 (copy from previous week)
- Mobile/network access with QR code support
- Improved formula adjustment (two-pass regex)
- All test cases passing

## File Naming Convention
`13WCF-Activity Rollforward App v{MAJOR}.{MINOR}.py`

When updating:
1. Increment MINOR for bug fixes and small changes (1.0 → 1.1)
2. Increment MAJOR for significant features or breaking changes (1.9 → 2.0)
3. Archive old version to Archive/Old_Versions_{DATE}/
4. Update this VERSION file
5. Update START_PYTHON_APP.command with new filename
6. Commit changes to git with version tag
